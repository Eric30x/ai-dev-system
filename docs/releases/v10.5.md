# V10.5 — Core Completion

**Released**: June 2026

## What shipped

### Bug Fixes
- 🐛 Artifact Download: Express 5 `res.download()` incompatible with Windows dot-directory paths → replaced with manual `createReadStream().pipe()`
- 🐛 syncFileTree: null bytes and surrogate pairs causing PostgreSQL JSONB errors → added sanitization
- 🐛 Artifact path storage: backslashes corrupting file paths → normalized to forward slashes
- 🐛 `workspace.writeFile()`: relative vs absolute path mismatch → unified to `path.resolve()`

### LLM Router
- 🔀 Unified provider detection: `DEEPSEEK_API_KEY`, `CLAUDE_API_KEY`, `OPENAI_API_KEY`, `GEMINI_API_KEY`
- 🔀 `.env` updated with explicit keys for all 4 providers

### Documentation
- 📚 Complete README rewrite (landing page style)
- 📚 6 docs files: architecture, agent-workflow, artifact-system, database, api, deployment
- 📚 CONTRIBUTING.md + CODE_OF_CONDUCT.md
- 📚 GitHub Issue/PR templates
- 📚 Release Notes (V9, V10, V10.4, V10.5)

## Verified

| Test | Result |
|------|--------|
| Monaco Editor | ✅ `monaco.editor.create()` real instance |
| Agent modifies files | ✅ `workspace.writeFile()` confirmed by diff |
| AI Fixer LLM calls | ✅ DB logs show real diagnoses |
| Artifact Download | ✅ HTTP 200, 10188B valid ZIP, 8 files |
| Rollback | ✅ Real extraction + backup |
| npm start | ✅ 8 routes loaded |
| Worker | ✅ Full pipeline executes |
